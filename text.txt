if static
	copy input into start of line up till newline
if newline in static
	clear up till first new line
if no new line in static
	if not at end of file
		read new input up till new line and copy input into line up until new line
	clear static
	copy remainder of input into static
return
